# OpenAI API Configuration
# Replace with your actual API key but never commit the actual .env file
OPENAI_API_KEY=your_openai_api_key_here

# Output Directory Configuration
# Optional: change the directory where results are saved
# OUTPUT_DIRECTORY=./results

# Model Configuration
# Optional: override the models to test (comma-separated)
# MODELS=gpt-4,gpt-3.5-turbo,gpt-3.5-turbo-instruct

# Test Configuration
# Optional: adjust test parameters
# TEMPERATURE=0.7
# MAX_TOKENS=1000
# REPETITIONS_PER_PROMPT=3
